# 文本溢出省略号

`text-overflow: ellipsis` 需要搭配使用：

```css
text-overflow: ellipsis;
/* white-space: normal; */
white-space: nowrap;
overflow: hidden;
```

如果是 `table`，还需要：

```css
table {
  table-layout: fixed;
}
```
